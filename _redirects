# File: netlify.toml
# Option 1 - Works for Netlify and cloud providers
[build]
  # Directory to deploy to GitHub pages  
  publish = '/app' 
  [build.env]
    # Add environmental variables here
  [build.environment] 
    # Add environment variables to the environment variable parsing group.
    APP_VSCODE_EXTENSION = GIST_NO_TOUQUE_FLEURT_FLEURT

# .netlify.toml
# Option 2 - For Netlify only, must be create at the repo root directory  
# /gandliergeant/filedeflaggerarit